# Home

Homepage-owned assets and scripts live here.

- Public page URL remains `/index.html`
- Root `index.html` may reference assets from this directory
- Canonical homepage assets should live in `web/home/assets/` and `web/home/scripts/`
- Legacy copies under `web/before/index/` are kept only as compatibility shims

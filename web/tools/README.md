# Tools

Developer tools page ownership boundary.

- Canonical page URL is `/web/tools/`
- Legacy entry `/web/before/html/tools.html` remains as a redirect shim
- `web/tools/` is the forward-looking source area for tool-specific code and assets
- Legacy public entries in `web/before/html/` now exist only as compatibility redirects
